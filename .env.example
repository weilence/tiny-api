# 数据库连接字符串
DATABASE_URL="postgresql://username:password@localhost:5432/api_doc"

# 是否允许用户注册 (true/false)
# 当设置为 false 时，注册页面将显示禁用提示，并且后端API会拒绝注册请求
ALLOW_REGISTER=true

# 系统初始化说明：
# 首次启动系统时，如果数据库中没有用户，系统会自动重定向到 /init 页面
# 在该页面创建第一个管理员账户后，系统即完成初始化
